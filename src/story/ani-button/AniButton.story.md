# AniButton组件

## 使用示例
```javascript
import { AniButton } from 'flyleaf';

const btn = new AniButton({
  target: document.getElementById('app'),
  props: {
    dataArr: ['过大年', '领福利'],
    duration: '2s',
    bgColor: '',
    color: '',
  }
})
```